# T3b UX round 1 — work log

- 2026-08-28 15:15 UTC — Started on clean `track/t3b-ux-round1`. Read `AGENTS.md`, the T3b spec, MIP-1 §3/§5, the amended `docs/contract.md`, the prior T3 track log, and the current session log. `DEVENV_ROOT` is unset in the invoking shell; `devenv 2.2.2` is available, so all Node/pnpm work will run through `devenv shell -- …`. The required committed track log did not exist and is created here. Next: audit the existing framework-free time/MRF core and shell, then implement the seven PO changes without changing the data contract.
- 2026-08-28 15:19 UTC — Completed the contract/data-model slice. `field` is now typed with its backwards-compatible `rain_rate` default; rain and radiation timelines are composed independently; a pure hourly selector joins the next 24 whole-hour slots with nearest rain/radiation frames (±30 min). `synthgen` now emits a sixth, 24-frame hourly radiation chunk with a spatially varied diurnal cycle and W/m² quant table. Early synchronous receipts from `web/`: `devenv shell -- pnpm synthgen` exit 0 (109 frames/6 chunks), `devenv shell -- pnpm typecheck` exit 0, `devenv shell -- pnpm test` exit 0 (3 files/8 tests, including field isolation, hourly selection, and radiation day/night decode). Next: build the histogram scrubber and shell UX.
- 2026-08-28 15:27 UTC — Implemented the seven PO UX changes. Layout choice: mobile is a vertical weather flow (compact header → map with overlaid search → current reading → histogram scrubber → hourly table), so the spatial answer stays first while details remain naturally scrollable; at ≥960 px the map owns the flexible left side and the information becomes a 400–520 px independently scrollable right rail. The rain histogram is now the only scrub surface (pointer capture, keyboard controls, fractional cursor, stale-frame suppression, visible verleden/nu/nowcast/model boundaries); the separate range input is gone. Added debounced PDOK suggest + lookup with keyboard navigation and non-blocking failure states. Theme cycles light (default) → system → dark, persists in localStorage, observes system changes, and swaps between filtered OpenFreeMap Liberty/Dark styles; all `transportation` and `transportation_name` layers are removed while water/place/boundary layers remain. The default De Bilt point makes the synthetic histogram/table immediately inspectable; map click, search, and geolocation replace it. Synchronous mid-pass receipts: `devenv shell -- pnpm typecheck` exit 0; `devenv shell -- pnpm test` exit 0 (5 files/10 tests); `devenv shell -- pnpm build` exit 0 (MapLibre bundle warning remains at 1,089 kB/297 kB gzip). Preview runtime at `127.0.0.1:4174`: `/` 200, manifest 200 with 5 rain + 1 radiation chunk, exact radiation Range request 206 with 32 bytes and correct Content-Range; live PDOK suggest returned results. No installed graphical/headless browser was found, so interaction and responsive visual checks are listed for manual verification in the final log entry.
- 2026-08-28 15:31 UTC — Published coherent implementation commit `7382297` to `origin/track/t3b-ux-round1` and opened draft PR https://github.com/mathijshenquet/motregen/pull/4. Final independent synchronous receipts from `web/`, run after that commit: `devenv shell -- pnpm synthgen` exit 0 (109 frames/6 chunks); `devenv shell -- pnpm typecheck` exit 0; `devenv shell -- pnpm test` exit 0 (6 files, 11 tests); `devenv shell -- pnpm build` exit 0. Build warning only: MapLibre keeps the main minified chunk at 1,089 kB (297 kB gzip), inherited from T3 and outside this UX round. Repro: `cd web && devenv shell -- pnpm synthgen && devenv shell -- pnpm typecheck && devenv shell -- pnpm test && devenv shell -- pnpm build`. Manual graphical verification steps: (1) run `cd web && devenv shell -- pnpm preview --host 0.0.0.0`; at desktop width confirm the road-free map fills the left side and the detail rail scrolls independently, then at ~390 px confirm map → reading → scrubber → table vertical order; (2) drag and tap across the histogram, then focus it and use arrows/Home/End—cursor, timestamp, intensity, and rain overlay must follow without a separate slider while verleden/nu/nowcast/model remain visible; (3) search “De Bilt”, use arrow keys + Enter, and confirm fly-to, marker, histogram, and table update; block the PDOK request and confirm the inline unavailable state leaves map picking usable; (4) cycle light → system → dark → light, confirm both chrome and basemap change, reload to confirm persistence, and change OS color scheme while on system; (5) inspect all 24 hourly rows—rain uses mm/u, sun uses W/m² with dim/zero night icons and brighter daytime icons; with the radiation chunk removed from a temporary manifest, confirm the entire Zon column disappears; (6) deny geolocation and confirm the Dutch recovery message; (7) confirm the droplet identity and `Bron: KNMI` attribution remain visible. End state is implementation-complete; graphical browser review remains explicitly with orchestrator/PO because this worker environment has no browser binary.
- 2026-08-28 15:36 UTC — Folded in the orchestrator's PO-HAR performance steer. Read `/home/mthq/motregen/tmp/click_har.log`: 166 Range entries over two location clicks, exactly 83 full-timeline frame requests per click (36 RTCOR + 25 nowcast + 22 harmonie). Root cause confirmed: the old 32-frame decoded LRU thrashed below the 83-frame rain timeline. The decoded LRU is now 256 frames (the composed synthetic rain+radiation timeline is 107 × 43,700 bytes ≈4.7 MB; full capacity ≈11.2 MB). Point-series sampling groups frames by chunk and calls `MrfClient.getFrames`; requests covering more than half a chunk fetch its entire payload in one Range and decode the needed independent members. New client-level test reproduces app state with the first header already loaded: cold location sample before = 83 payload Range requests in the PO HAR; after = 11 total Range requests (6 whole payloads + 5 remaining headers); second location after = 0 network requests because all 107 decoded frames remain cached. `build.sourcemap: true` is set; `devenv shell -- pnpm build` exit 0 and emitted a 2,504 kB main JS sourcemap. Synchronous development receipts: `devenv shell -- pnpm typecheck` exit 0; `devenv shell -- pnpm test` exit 0 (6 files/12 tests, including the exact 11→0 fetch-count assertion); `devenv shell -- pnpm build` exit 0. Next: commit/publish this follow-up, then run the full final gate sequence once more on the new head.
- 2026-08-28 15:37 UTC — Committed the HAR follow-up as `1d1a4d0`. Final full synchronous gate sequence from `web/` after the implementation commit: `devenv shell -- pnpm synthgen` exit 0 (109 frames/6 chunks); `devenv shell -- pnpm typecheck` exit 0; `devenv shell -- pnpm test` exit 0 (6 files/12 tests); `devenv shell -- pnpm build` exit 0 with sourcemaps present. The only warning remains the known MapLibre main-chunk size (1,090 kB minified/298 kB gzip; 2,504 kB map). Implementation and automated verification are complete; next step is publish the two local follow-up commits after the transient GitHub DNS failure and verify PR #4's head.
