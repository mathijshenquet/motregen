# Track T2e — seamless blend

## 2026-08-28

- 2026-08-28 20:40 UTC — Gestart vanaf schone `track/t2e-seamless` op mergecommit `4027fdf` (T2d). `direnv status` bevestigt dat `.envrc` toegestaan en geladen is; sleutels blijven uitsluitend in `.env`. Doel: het operationele KNMI-seamlessproduct ontdekken, als `rain_rate` op het gedeelde grid publiceren, T2d-motionannexen en generatiegesuffixte chunknamen toepassen, onafhankelijk valideren en de broncompositie tot +6:30 u bewijzen. Volgende stap: officiële datasetmetadata en voorbeeldbestand met de geregistreerde sleutel onderzoeken, daarna decoder-/pipelinegrenzen vastleggen.
- 2026-08-28 20:36 UTC — Correctie op de eerste handmatige logtijd: `20:40` was een UTC-typefout; feitelijke start was circa 20:32 UTC. Officiële catalogus + live API bevestigen `seamless_precipitation_ensemble_forecast_members` 1.0 en `seamless_precipitation_ensemble_forecast_probabilities` 1.0, beide NetCDF en iedere vijf minuten gepubliceerd. Vijf actuele membersfiles zijn 54,0–59,9 MB; probabilities 8,5–9,3 MB en zijn conform scope alleen gelijst, niet gedownload/opgeslagen. Een membersfile van exact 54.027.354 bytes is synchroon met geregistreerde sleutel opgehaald. Structuur: 20 realizations (1…20), 72 leads (+5…+360 min), 780×780 regulier oplopend lat/lon-grid (`lat 48,9955…56,0065`, `lon −0,00725…11,28825`), `uint16 precip_intensity`, schaal 0,01 mm/u, fill 65535 en gzipchunks per member/tijd. Geen metadata markeert een controle- of mediaanmember; besluit daarom: ensemblemediaan per cel, bij 20 geldige leden het gemiddelde van rang 10 en 11. Productiedecoder leest alleen leads ná de ingestelde nowcasthorizon. Een 137.280-byte 4×5-crop met alle members/tijden en natte medianen is als conformancefixture gemaakt; onafhankelijke h5py-referentie toegevoegd. Omgevingsmuur: de overgeërfde shell heeft geen `cc`; `direnv exec .` meldt `.envrc` geblokkeerd en `devenv shell` kon door tijdelijke Nix-DNS geen profiel realiseren. Implementatie gaat door; finale Rust-receipts wachten op werkende activatie.
- 2026-08-28 21:01 UTC — Orchestrator heeft `direnv allow` uitgevoerd; alle volgende receipts lopen rechtstreeks via `direnv exec .` (bevestigd `cc` uit het devenv-profiel). Implementatie gereed: NetCDF4/HDF5-decoder valideert schema/calibratie/run en berekent per lead de celmediaan; reguliere lat/lon-indexmap projecteert naar het gedeelde 1.250×1.350-grid; publisher schrijft `seamless`-regen met T2d's `encode_rain_with_motion` en generatiehash. Daemon initialiseert de bron verplicht, behoudt bij refreshfouten de vorige chunk en publiceert manifest/chunks via het bestaande atomaire pad. Manifestvalidator heeft nu een expliciete bronenum inclusief `seamless` en eist daarvoor `rain_rate`. Gerichte synchrone tests na één gecorrigeerde float-gridvergelijking: `direnv exec . cargo test -p knmi-hdf5 -p motregen-ingest` exit 0; h5py en Rust zijn cel-exact gelijk voor alle 72 fixtureleads.
- 2026-08-28 21:01 UTC — Cadanssteer verwerkt: native 5-min download kost bij gemeten 54,0–59,9 MB circa 15,6–17,3 GB/dag; `MOTREGEN_SEAMLESS_CADENCE` is daarom standaard 15 min (5,2–5,8 GB/dag, 3× lager), terwijl iedere check het nieuwste vijfminutenbestand kiest. Twee echte +125…+360-verwerkingen duurden 256,7 en 258,4 s onder gelijktijdige daemonbelasting, ruim binnen 900 s. Motivering en 30-dagenbytes staan in `docs/seamless.md`.
- 2026-08-28 21:01 UTC — Finale live daemonreceipt op de cadencecode, synchroon exit 0: `direnv exec . bash -c 'set -o pipefail; RUST_LOG=info cargo run --release -p motregen-ingest -- --once --history-hours 1 --nowcast-minutes 120 --arome-hours 10 --data-dir data/t2e-e2e 2>&1 | tee /tmp/motregen-t2e-final-daemon.log; status=${PIPESTATUS[0]}; printf "final_daemon_exit=%s\\n" "$status"; exit "$status"'` → `final_daemon_exit=0`. Manifest: 12 chunks, 5 bronnen, 9 velden, 3 grids. Seamlessfile `seamless-20260828T2045-m125-360-g4f5cf404929ac268.mrf` is 1.543.562 B, 48 frames (+125…+360), `motion_grid=40×43`, 47/47 mogelijke annexen en dus zowel T2d-motion als generatiesuffix bewezen. Spotcheck op eerste/middelste/laatste frame: ieder 1.610.000 geldige + 77.500 no-data-cellen; maxfout 0,034864/0,016436/0,031294 mm/u, alle binnen één lokale quantstap. `uv run --project spec spec/validate_manifest.py data/t2e-e2e` en `spec/spot_check_seamless.py` beide exit 0.
- 2026-08-28 21:01 UTC — Tijdlijnbewijs uit het finale manifest met `now=20:45Z`, exact volgens contractprioriteit en clientinterpolatie, alle 79 vijfminutenslots +0…+6:30: +0 = RTCOR (1 slot, gemeten wint); +0:05…+2:00 = nowcast (24); +2:05…+6:00 = seamless (48); +6:05…+6:10 = blend tussen laatste seamless- en eerstvolgende HARMONIE-frame (2); +6:15…+6:30 = HARMONIE (4). Hiermee is de harde nowcast→uurmodelknik vervangen door de 5-min-blend en blijft de overgang na +6 u eveneens interpoleerbaar. Volgende stap: volledige fmt/clippy/test/pyright-gates, diffreview en commit/publicatie.
- 2026-08-28 21:04 UTC — Finale gates op de volledige werkboom, synchroon groen via `direnv exec .`: `cargo fmt --check`; `cargo clippy --all-targets -- -D warnings`; `cargo test` (alle Rust-unit-, integratie-, property- en doctests, inclusief h5py-conformance en de expliciete 120→48-framegrens); afsluitend `final_rust_exit=0 diff_check_exit=0`. Python parallel: `uv lock --check --project spec`; pyright op alle negen specscripts inclusief seamless (`0 errors, 0 warnings`); live manifestvalidator exit 0. `git diff --check` schoon; `.env` is genegeerd en niet gevolgd. Implementatie, documentatie, fixture en dit verplichte log zijn commitklaar.
- 2026-08-28 21:05 UTC — Implementatiecommit `59bbd30e7dbf1e3bff971d4278a6e97eb049e5b5` gepubliceerd; `git ls-remote` bevestigde exact dezelfde remote-SHA. Draft-PR #13 geopend: https://github.com/mathijshenquet/motregen/pull/13. Deze publicatieregel wordt als verplichte log-follow-up gecommit; daarna remote/PR-head nogmaals verifiëren.
