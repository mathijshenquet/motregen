# Track U24 — windparticles: korrel, wegvallers, loefzijde, continu zoomen, subtieler default

- 2026-09-25 08:40 UTC — Start (claude opus 5.5, worktree `track/u24-wind-kwaliteit` op main `30992e3`). Spec, AGENTS.md, `wind-layer.ts`, `wind-layer-viewport.test.ts`, `wind-zoom.spec.ts`, `focus-mode.ts` (`WIND_FOCUS_GAIN` 3/2) en LOGs U3/U3b/U12/U20 gelezen. `direnv allow`, `pnpm install --frozen-lockfile` in `web/`. Volgorde zoals spec: (1) default subtieler, (2) korrel, (3) wegvallers, (4) loef, (5) continu zoomen.
- 2026-09-25 08:50 UTC — Meetscript `web/scripts/wind-quality.ts ORIGIN OUT LABEL` (Playwright, swiftshader, 1280×800 DPR 2 = Mac-profiel, live data via `vite preview` + `MOTREGEN_DATA_ORIGIN=https://motregen.nl/data`, z7 op 52,2/5,2, adaptief budget uit, afspelen gepauzeerd). Inkt = alpha van de windcanvas zelf, teruggelezen (`readPixels`) direct na elke windrender — dus ná de composite met intensiteit — gesplitst naar land/zee via het basisbeeld zonder wind; dekking = aandeel pixels met alpha > 8/255. Scenario's `ink`, `crop` (200 %-uitsneden), `zoom` (continu 7→9 in 100 stappen van 0,02, één per animatieframe, tegen één sprong 7→9) en `profile`. Zoom draait op een **virtuele klok** (elk animatieframe = 1/60 s): swiftshader + readback haalt ~5 fps, en de trailbuffer vervaagt per seconde, dus zonder virtuele klok krijgt een trail 10× minder warps per levensduur dan op een Mac. Baseline-build = main `30992e3` (`dist-base`, :4371).
- 2026-09-25 08:55 UTC — **Punt 1, meting** (`metingen/sweep-i*.json`, baseline-build met `WIND_TUNING` v3, donker/licht): landinkt (gem. alpha) bij intensiteit 1,27 (huidige default) 3,96/4,03 ‰; 0,85 → 2,55/2,54 (0,64×); **0,75 → 2,38/2,39 (0,60×)**; 0,70 → 2,14/2,14 (0,54×); 1,905 (= focus) 5,81/5,86. Inkt is vrijwel lineair in de intensiteit (klemming bij kop-alpha > 0,79 speelt nauwelijks). Dekking > 8/255 daalt minder (1,57 → 1,35 %): de dunne staarten blijven zichtbaar, de koppen worden zachter. Host-load bij deze sweep 16,5 bij de start — vanaf nu load-poll < 16 vóór elke Chromium-run.
- 2026-09-25 08:58 UTC — **Punt 1, fix**: default `intensity` 1,27 → 0,75 (0,60× landinkt). Nieuw `WIND_FOCUS_INTENSITY = 1,905` (U19: 1,27 × 1,5) en `WIND_FOCUS_GAIN = WIND_FOCUS_INTENSITY / default` (= 2,54), zodat windfocus met de default exact op de oude focusinkt uitkomt. Opgeslagen v3-afwijkingen blijven staan (dus een eigen intensiteit krijgt nu ×2,54 in focus i.p.v. ×1,5 — bewuste keuze: de focusversterking is gedefinieerd t.o.v. de default). v2-migratie: 1,27 (U19-default in het v2-tijdvak) toegevoegd aan de "was een default"-lijst; een eigen v2-intensiteit wordt de focussterkte (schaal 0,75/1,905 i.p.v. ⅔). e2e `focus.spec.ts`: rust 0,75, focus 1,905. `vitest run src/core/{focus-mode,wind-layer,dev-settings}` exit 0 (45 tests); `pnpm typecheck` exit 0.
- 2026-09-25 09:05 UTC — PO-queue (MIP-12 dev-opties): elke dev-knop/variant die ik toevoeg krijgt hier een eigenaar + vervaldatum; geen losse `?`-URL-parameters (alleen toggles binnen `?dev`); windknoppen die ik tot constante maak verwijder ik in deze merge. Tot nu toe: **geen** nieuwe knoppen of parameters; meetvarianten (fp16-emulatie, RGBA16F, ankermodel) zijn wegwerpbuilds in scratch, niet in de code.
- 2026-09-25 09:25 UTC — **Punt 3 (wegvallers), meting VÓÓR** (headless probe op de life-logica, nepkaart 1280×720 z8, 600 frames opwarmen, 1200 frames meten, met en zonder zoomstappen ±0,5 elke 2 s; "abrupt" = kop met alpha ≥ drempel in frame n die in frame n+1 niet meer bestaat of alpha 0 heeft, alleen binnen beeld; `metingen/wegvallers-probe.txt`). Drempel 64/255, rust/met zoom: uniform 5 m/s 15,0/14,5 per s (vrijwel allemaal koppen die de lijrand passeren); uniform 1 m/s 3,0/1,7; **veld met snelheidsblokken 0–8 m/s 144/127**; **veld met NaN-stroken 93/70**. Oorzaak: `remaining` = min(afstand, momentane snelheid × resttijd) — een sprong naar (bijna) windstil of een NaN-cel (snelheid 0) zet de kop in één frame op 0; en `outside`/`die → removeSlot` wist zonder ramp.
- 2026-09-25 09:30 UTC — **Punt 3, fix**: (a) de getoonde kop-alpha (`shown`) mag omhoog direct maar omlaag hooguit 1/0,35 s per seconde (`DEATH_SECONDS`); (b) een dood (einde leven, buiten beeld, overtal-ramp op 0) met zichtbare kop (≥ 1/255) wordt een **uitloper**: hij drijft door, dooft lineair uit, telt als overtal (`retiring`, niet in de cellentelling) en de vervanger wordt meteen in een nieuw slot geboren; pas bij `shown` = 0 gaat het slot weg (`retire`). Een overtal-ramp die op 0 komt terwijl de begrensde alpha er nog boven zit, loopt ook eerst uit (eerste versie gaf daar nog 24/s bij drempel 13). **NA**: drempel 64 overal 0,00/s; drempel 13 ≤ 0,85/s (dat is de laatste stap 13→0 van de lineaire uitloop zelf). Regressietest in `wind-layer-viewport.test.ts` ("never lets a visible head vanish…", blokken + NaN-stroken met zoomstappen, verwacht 0) — geverifieerd dat hij op de oude `wind-layer.ts` faalt (blokken: 2621). Bestaande U12-tests tellen uitlopers apart (`settled()`/`surplus()`): de invariant `active === budget` geldt nu voor de levende particles. `vitest run src/core/wind` exit 0 (36 tests).
- 2026-09-25 09:21 UTC — Correctie: de twee vorige entries ("09:25", "09:30") zijn geschreven om ~09:17 UTC; ik had de tijd geschat i.p.v. gelezen. Vanaf nu `date -u`.
- 2026-09-25 10:04 UTC — **Punt 2 (korrel), metingen** (dark, z7, DPR 2; builds in scratch, niet in de code: `v0` = punt 1, `f16` = v0 met fp16-afronding van alle UV's in fade/composite (emulatie van `mediump` op Apple-GPU's), `h16` = v0 met RGBA16F-trailtargets; `dpr2` = v0 met tuning `bufferDpr: 2`). Gem. alpha / dekking > 8/255: v0 2,23 ‰ / 1,38 %; **dpr2 2,26 ‰ / 1,14 %** (zelfde inkt in minder pixels = scherper; piek-alpha in de drukste uitsnede 98 → 182); h16 2,24 ‰ / 1,38 % (geen meetbaar verschil, staarten iets gladder); f16 2,02 ‰ / 1,36 % (−10 % inkt, onderbroken staarten in de uitsnede) en bij continue zoom min 0,50× i.p.v. 0,42×/ eind 0,60×. Uitsneden alleen-wind ×3: `shots/b2-{v0,dpr2,cur}-dark-wind-x3.png`, `shots/g-{f16,h16}-dark-wind-x3.png`; kaart op 200 %: `shots/b2-{v0,dpr2}-dark-crop-zee.png`.
- 2026-09-25 10:04 UTC — **Punt 2, fix**: (1) default `bufferDpr` 1,5 → 2: een 2×-Mac krijgt een buffer op volle resolutie i.p.v. 0,75× LINEAR opgeschaald; Pixel 5 (DPR 2,75) gaat van 0,55× naar 0,73× per as (≈1,8× pixels) — 4G-gate in `perf.spec.ts` moet dat bevestigen. (2) fade- en compositeshader `highp` (in `mediump`/fp16 is een UV op een buffer > 2048 px tot een halve texel onnauwkeurig — de f16-emulatie laat onderbroken staarten zien; op echte Apple-hardware niet door mij geverifieerd). (3) fade zonder herprojectie = exacte texelkopie (`texelFetch`). RGBA16F niet ingevoerd: geen meetbare winst voor 2× geheugenbandbreedte. `antialias: false` + shader-AA (U3b): kop-AA in bufferpixels klopt, want `halfWidth` wordt nu expliciet omgerekend naar de verankerde buffer (punt 5). Geen nieuwe knoppen; `bufferDpr` bestond al.
- 2026-09-25 10:04 UTC — **Punt 4 (loefzijde), meting**. Metric `windProfile()` (koppendichtheid in 10 banden van gelijk oppervlak langs de gemiddelde wind, loef/lij = buitenste 20 %), in de perf-JSON-export (`?perf=1` → "Kopieer JSON" → `wind`), plus in `wind-quality.ts` een inkt-profiel uit de readback. Sim (nepkaart, uniform veld): koppen loef/lij **0,91–0,94** (west 1/5/12 m/s, zuidwest 6) — al ≥ 0,8. In-app (live, vandaag zuidenwind ~3,4 m/s, z7): inkt loef/lij **1,21** (`metingen/p-base.json`). Varianten (constanten, wegwerpbuilds): (a) loefgewicht = meest loefwaartse van k lege kandidaatcellen: sim k=2 1,20–1,99, k=4 1,6–5,6, k=8 tot ∞ (lij loopt leeg), in-app k=2 inkt **3,34**; (b) randinstroom (deel van de respawns volwassen net buiten de loefrand, instroom ∝ binnenwaartse windcomponent): sim 5 % 0,93–1,19, 10 % 1,02–1,37, 15 % 1,15–1,62; in-app 10 % inkt **1,59** (eerste band 1,69×). **Besluit**: geen van beide ingevoerd — het tekort is in sim noch app te reproduceren en beide overcorrigeren; code weer verwijderd, de meting blijft. **Vraag PO**: bij welk weer/welke plek zag je de lege loefzijde? Een "Kopieer JSON" (`?perf=1`) op dat moment geeft het profiel (`wind.ratio`, `wind.inkRatio`).
- 2026-09-25 10:04 UTC — **Punt 5 (continu zoomen), meting VÓÓR** (baseline main, virtuele klok, `metingen/base.json`): tijdens een continue zoom 7→9 zakt de windinkt naar **0,42×** rust en blijft daar (steady ~0,45×); eind continu / eind sprong = 2,08/3,22 = **0,65**. Oorzaak: elke frame warpt de fadepass de hele trailbuffer met LINEAR-hersampling; die vervaging spreidt een staart over meer pixels en de lineaire vloer (1/255 per frame per pixel) vreet dan meer inkt weg. `viewportParticleRetention` speelt bij inzoomen niet (retentie = 1); bij uitzoomen is het product van de stapretenties exact gelijk aan de netto retentie, dus accumuleren verandert daar niets.
- 2026-09-25 10:04 UTC — **Punt 5, fix (ankermodel)**: de trailbuffer blijft verankerd in het beeld van zijn laatste herprojectie (`trailView`); koppen worden via die transformatie in de ankerbuffer getekend (lijnbreedte omgerekend), de composite toont de buffer met de transformatie, en herprojecteren (één bilineaire hersampling, met de netto retentie) gebeurt alleen bij stilstand of als het anker > 0,5 zoom of > 5 % UV-marge afwijkt. Een tweede tekening binnen 4 ms (kaartrender + eigen frame) laat de tijd niet lopen: geen dubbele vloer, geen nul-segment. **NA** (`metingen/z-anchor.json`): min tijdens gesture **0,68×** rust (was 0,42×), eind continu / eind sprong = 1,78/1,99 = **0,89** (doel ≥ 0,70). De resterende dip is het aandeel aanvullers dat nog infadet (0,25 s, U12/U20-keuze). e2e `wind-zoom.spec.ts` kreeg de continue case (readback van de windcanvas per frame, virtuele 60 Hz-klok): min > 0,5× rust en eind > 0,7× sprong.
- 2026-09-25 10:55 UTC — Rebase op main `2cee6eb` (bevat 211b57fb; PO-queue e2e-slots). Metingen en e2e vanaf dan via `scripts/e2e-slot.sh`. **Gericht gedraaide e2e-specs** (eigen en geraakt): `e2e/wind-zoom.spec.ts` (nieuwe continue case), `e2e/focus.spec.ts` (windfocus 0,75 → 1,905), `e2e/perf.spec.ts` (4G-gate i.v.m. buffer-DPR 2). Niet de volledige suite (orkestrator). Contra-check: dezelfde continue-zoom-test tegen main's `wind-layer.ts` (plus exportshims zodat `tsc -b` bouwt; een eerste poging zonder shims faalde op de build en telt niet) → exit 1: continu eind 1,18 tegen sprong 2,08, min 0,499× rust. De min-drempel (0,5) is daar krap; het eind-criterium (0,7× sprong; oud 0,56) is het echte onderscheid.
- 2026-09-25 10:55 UTC — **Eindmeting** op de gate-build (`metingen/fin.json`, `fin-z.json`): landinkt donker/licht 2,30/2,37 ‰ tegen 3,96/4,03 ‰ bij de oude default = **0,58/0,59×** (doel 0,55–0,65; buffer-DPR 2 veranderde de inkt niet meetbaar); windfocus = 1,905 = oude focusinkt. Continue zoom 7→9: min **0,75×** rust, eind continu/sprong 1,95/1,68 = **1,16** (doel ≥ 0,70). Inktprofiel loef/lij 1,21 (zuidenwind ~3 m/s).

## Slot U24

| # | klacht PO | oorzaak (gemeten) | fix | vóór → na |
|---|---|---|---|---|
| 1 | te intens zonder selectie | — | default `intensity` 1,27 → 0,75; `WIND_FOCUS_INTENSITY` 1,905 (gain = 1,905/default) | landinkt 1,00 → 0,58–0,59×; focus gelijk |
| 2 | korrelig | 2×-Mac: buffer 0,75× (bufferDpr 1,5) LINEAR opgeschaald; mediump-UV (fp16) op Apple-GPU's tot ½ texel mis (alleen geëmuleerd) | `bufferDpr` 2; fade/composite `highp`; exacte texelkopie in rust | dekking 1,38 → 1,14 % bij gelijke inkt (scherper); uitsneden `shots/b2-{v0,dpr2,cur}-dark-wind-x3.png` |
| 3 | particles vallen weg | `remaining` op momentane snelheid: sprong naar ~0/NaN doodt de kop in één frame; dood/overtal zonder ramp | kop-alpha daalt hooguit 1/0,35 s; dode koppen lopen uit, vervanger direct | abrupt (≥ 64/255) 15–144/s → 0/s (unit-regressietest) |
| 4 | te weinig aan loefzijde | niet gereproduceerd: koppen loef/lij 0,91–0,94 (sim), inkt 1,21 (app, vandaag) | geen; beide varianten gemeten en verworpen (overcorrectie 1,6–∞); meting `windProfile` in perf-JSON | — |
| 5 | continu inzoomen: trails weg | per-frame LINEAR-warp van de hele buffer + lineaire vloer | ankermodel: herprojecteren alleen bij rust/> 0,5 zoom/> 5 % marge; geen trailpass bij dubbele tekening | min 0,42 → 0,75× rust; eind/sprong 0,65 → 1,16 |

**Receipts (HEAD vóór deze LOG-commit, code = `c2f4f40`)**: `pnpm typecheck` exit 0; `pnpm test` exit 0 (42 bestanden, 247 tests); `pnpm build` exit 0; `MOTREGEN_E2E_PORT=4364 MOTREGEN_E2E_DATA_PORT=8364 pnpm e2e e2e/wind-zoom.spec.ts e2e/focus.spec.ts e2e/perf.spec.ts` (e2e-slot, load 15,2 bij start) → FULL-GATE-EXIT 0 (14 passed, 22 skipped, 4,9 min); 4G warme TTFR 1510 ms, koud 2133 ms.

**Dev-knoppen/varianten (MIP-12)**: geen nieuwe knoppen of `?`-parameters. Nieuwe dev-uitvoer: `wind` (loef/lij-profiel) in de perf-JSON-export — eigenaar U24, verdwijnt bij PO-keuze in deze merge óf blijft als meting voor de vervolgvraag bij punt 4 (PO). Knoppen tot constante gemaakt: geen (`bufferDpr` blijft knop, alleen default). Meetscript `web/scripts/wind-quality.ts` blijft (dev-tool, zoals `wind-zoom.ts`).

**Open met PO**: (a) punt 4 — bij welk weer/welke plek zag je de lege loefzijde? Graag "Kopieer JSON" (`?perf=1`) op dat moment; (b) punt 2 op echte Mac-hardware beoordelen (fp16-effect alleen geëmuleerd); (c) een eigen opgeslagen intensiteit krijgt nu ×2,54 in windfocus (was ×1,5). **Open voor agents**: de resterende dip tijdens continu zoomen (~0,75×) is de 0,25 s-fade-in van aanvullers; volledige e2e-suite op de merge-kandidaat (orkestrator).
