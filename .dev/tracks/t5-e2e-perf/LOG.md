# T5 e2e/performance — work log

- 2026-08-31 10:50 UTC — Gestart op schone `track/t5-e2e-perf` vanaf `f6502f0`. Gelezen: `AGENTS.md`, de volledige T5-specificatie, MIP-7, MIP-3/headercontract, `docs/contract.md`, `Caddyfile.dev` en de T3b/T3e/T3f-weblogs. Direnv/devenv is actief; Node 24.19.0 en pnpm 11.21.0 zijn beschikbaar. Conflictgrens wegens parallelle T3g: nieuwe perfcollector/HUD/e2e-bestanden, alleen smalle imports/hooks/mount in `App.tsx`, package/lockfile, `docs/perf.md` en `scripts/smoke.sh`; bestaande componenten en kernmodules blijven verder onaangeraakt. Meetdefinitie: TTFR = `performance.timeOrigin`/navigation start tot de eerste MapLibre-`render` ná upload van een regenframe; scrub-latency = laatste expliciete histograminput tot de MapLibre-`render` die het niet-verouderde bijbehorende regenframe commit. De collector wordt via één gedeelde browser-API door HUD en Playwright gelezen. Volgende stap: collector met unit-/overheadtest en HUD, daarna de minimale App-integratie.
- 2026-08-31 10:57 UTC — Framework-vrije `PerfMonitor` en mobiele HUD in nieuwe bestanden geïmplementeerd. Collector: TTFR, begrensd venster van 256 scrubmetingen met p50/p95, lopende rAF-fps, Resource Timing uitgesplitst in manifest/chunks/tiles/overig+totaal, en manifestleeftijd; publieke `window.__motregenPerf` levert dezelfde JSON-snapshot aan HUD en e2e. Verborgen HUD pollt niet; collectorwerk per frame is alleen tellers/tijdvergelijking, Resource Timing en percentielsortering gebeuren pas bij een snapshot (HUD zichtbaar: 2 Hz). App-diff beperkt tot imports, manifesttimestamp, MapLibre-rendercommit, scrubwrapper, triple-tap en mount. `?perf=1` werkt direct; triple-tap wisselt de overlay. Synchronous receipts vanaf `web/`: `pnpm typecheck` exit 0; `pnpm test -- src/core/perf.test.ts` exit 0 en draaide de volledige suite: 19 bestanden/58 tests. Volgende stap: Playwright toevoegen, zelfstartende synth-previewconfig en functionele/netwerkbudgetscenario's bouwen en kalibreren.
- 2026-08-31 11:22 UTC — Playwrightlab opgebouwd en gekalibreerd. NixOS vereist de Nix-browser i.p.v. Playwrights Ubuntu-download; `devenv.nix` levert daarom `pkgs.playwright-driver.browsers`, en de lockfile resolveert npm `@playwright/test` naar 1.61.1 omdat die net als de gelockte Nix-browser Chromium-revisie 1228 verwacht. Suite start zelf synthgen, productiebuild, preview en een Caddy-dataserver op 8185; een nieuwe lokale achtergrondstyle vermijdt externe basemapruis zonder Playwright-routing (routing schakelt Chromium-cache uit). Budgetkalibratie: cold bleef 2.000 ms; warmvoorstel 500 ms was met gemeten 520–600 ms te krap en is 750 ms; sessie 8 MB en scrub <1 transfer/3 frames bleven ongewijzigd. De gate vond een echte scrubstorm: snelle inputs startten losse frame- én motionranges vóór een commit kon prefetchten. Expliciete scrubinput coalescet nu de zes nabije frames via één gedeelde volledige immutable chunkrange (incl. motion), met een kortlevende compressed-payloadpromise; normale initial render behoudt kleine indexranges en de decoded LRU blijft begrensd. Unit/typecheck na de fix: `pnpm typecheck` exit 0; `pnpm test` vond eerst terecht de nieuwe Playwright-spec via Vitests glob, waarna `e2e/**` expliciet is uitgesloten; herhaling volgt in de finale gates.
- 2026-08-31 11:31 UTC — Live-smoke en nulmetingen afgerond. `scripts/smoke.sh` controleert index/manifest 200, versheid <900 s, MIP-3 Cache-Control/CORS/exposed headers/ETag/Accept-Ranges, immutable chunks zonder Content-Encoding en echte 0–7 Range→206; synchrone receipt tegen de bestaande echte-datafrontend: `./scripts/smoke.sh http://127.0.0.1:4173` exit 0 (`age=581s`). Echte-data-HUD gemeten via eigen productiepreview :4186 → Caddy :8080, lokale basemap, Chromium/SwiftShader 1280×720: TTFR 1.141,2 ms; scrub 13 commits p50/p95 69,1/172,7 ms; stabiele fps-indicatie 60; 110 Resource Timing-requests/20.118.133 transferbytes, waarvan chunks 104/19.772.524; 0 fouten. Synth flakiness-check na finale scrubfix tweemaal direct achter elkaar synchroon groen: run 1 `devenv shell -- pnpm e2e` exit 0, cold/warm 461,3/520,2 ms, scrub 23/83 transfers en p50/p95 10,0/19,3 ms, sessie 1.310.182 B, fps 45–58; run 2 exit 0, cold/warm 474,9/548,4 ms, scrub 23/83 en 8,2/21,9 ms, sessie 1.310.182 B, fps 40–56. Collector-overheadbenchmark: 1.000.000 framecallbacks 7,14 ms (0,007 µs/stuk); 10.000 snapshots met 120 resources 549 ms (0,055 ms/stuk). Alles en de budget-/ruisverklaring staat in `docs/perf.md`. Volgende stap: finale standaardgates, diffreview, commit en draft-PR.
- 2026-08-31 11:41 UTC — Finale review/gates afgerond. Vitest-uitsluiting corrigeert nu expliciet `configDefaults.exclude + e2e/**`; een eerdere samengestelde shellreceipt was ongeldig als totaalbewijs omdat een losse newline na de falende test alsnog smoke startte, dus alle receipts zijn daarna apart of onder `bash -c 'set -e; …'` herhaald. Finale synchroon: `pnpm build` exit 0 (alleen bestaande MapLibre chunkwaarschuwing; JS 1.136,09 kB/311,70 kB gzip), `pnpm typecheck` exit 0, `pnpm test` exit 0 (19 bestanden/58 tests), finale extra `devenv shell -- pnpm e2e` exit 0 (cold/warm 473,7/564,7 ms; scrub 23/83, p50/p95 10,5/32,2 ms; sessie 1.310.182 B; fps 46–57), en `bash -n scripts/smoke.sh && scripts/smoke.sh http://127.0.0.1:4173` exit 0 (`age=121s`). `git diff --check` schoon. Parallelle T3g-footprint gecontroleerd: T3g wijzigt `App.tsx`, HistogramScrubber, windlaag en `styles.css`; T5 raakt daarvan uitsluitend het vooraf verwachte `App.tsx` en laat T3g's componenten/styles ongemoeid. Initiële De Bilt-sampling start nu éénmalig ná het initiëren van de eerste framefetch (TTFR meet geen volledige puntserie) en reset niet bij thema-style reload. Volgende stap: alles inclusief dit LOG committen, publiceren, draft-PR openen en remote head verifiëren.
