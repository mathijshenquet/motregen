# T1 — AROME GRIB spike

- 2026-08-28T15:00Z — Resumed/started track. No prior LOG, no uncommitted changes, and no track-specific commits were present (`HEAD` at `4f4b8fc`). Reading accepted MIP context and inventorying the environment before fetching data. Next: inspect API listing/file sizes and establish the Python/Rust implementation path.
- 2026-08-28T15:08Z — Read MIP-1 §3/§5 and MIP-2 in full. Activated devenv (required local Nix-daemon approval): rustc 1.97.1, cargo 1.97.0, uv 0.12.5, ecCodes/libeccodes 2.48.0, Python 3.14.7. No package change needed yet. Next: inspect authenticated AROME API listing and sizes before downloading.
- 2026-08-28T15:14Z — Authenticated listing showed the ten newest artifacts are hourly tar bundles, 866,897,920–868,034,560 bytes. Selected newest `HARM43_V1_P1_2026082812.tar`, advertised as 867,942,400 bytes. First transfer stopped at 623,624,192 bytes and retained `.part`; next: resume with a fresh presigned URL and validate the exact size.
- 2026-08-28T15:24Z — Resumed download and validated exact 867,942,400-byte archive. It contains 61 uncompressed extensionless GRIB1 files, one per lead +0…+60; file sizes are 11,986,144 bytes (+0) and roughly 14.2–14.3 MB thereafter. Each has 49 messages on a 390×390 `regular_ll` grid. ecCodes lacks names for KNMI local table 253, but raw inventory identifies total precipitation as table 253 / indicator 61 / surface / accumulation from 0 to lead; +1 statistics match component parameter 181 and conventional GRIB1 parameter 61 semantics. Extracted +0…+24 (25 files, 337 MB). Rule: hourly mm = accumulated kg/m² at N minus N−1 (1 kg/m² = 1 mm); +1 uses its accumulation directly. Next: run the cfgrib exporter and inspect crate API.
- 2026-08-28T15:38Z — Added `spec/` uv project and cfgrib/xarray exporter. With `ECCODES_DIR` it synchronously exported a 390×390 float32 `.npy` plus JSON grid/selector metadata from +1. Added workspace and `knmi-grib` library using `eccodes` 0.14, plus an integration test that invokes the Python reference and requires exact float32 elementwise equality. First Rust build found libeccodes through pkg-config but failed because eccodes-sys runs bindgen and libclang was absent; added nixpkgs libclang plus `LIBCLANG_PATH` to devenv. Next: rebuild and resolve compiler/conformance results.
- 2026-08-28T15:53Z — FFI build fixed with libclang plus glibc include path. Exact full-field conformance is green (152,100/152,100 float32 values; no tolerance). Added checked hourly de-accumulation and release benchmark. Five warm wall-time receipts for decoding/de-accumulating +1…+24: 0.20, 0.19, 0.18, 0.19, 0.18 s (median 0.19 s), well below 2 s. Wrote `docs/arome.md`, including honest residual risks and passed verdict. Final synchronous receipts: `direnv exec . cargo fmt --check` exit 0; `direnv exec . cargo clippy --all-targets -- -D warnings` exit 0; `direnv exec . cargo test` exit 0; `direnv exec . uv run --project spec --with pyright pyright spec/export_fixture.py` exit 0. Next: inspect diff, commit, push, and open draft PR per repository policy.
