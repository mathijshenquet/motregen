# T3 frontend shell — work log

- 2026-08-28 15:10 UTC — Resumed after reported re-auth failure. No prior track log, commits, untracked files, or worktree changes existed; branch was clean at shared seed `4f4b8fc`. Read AGENTS.md, MIP-1, MIP-2, `docs/contract.md`, and the T3 spec in full. Confirmed direnv/devenv active (Node 24.19.0, pnpm 11.21.0). Starting scaffold and contract-first core implementation.
- 2026-08-28 15:29 UTC — Scaffolded Vite/Solid/Tailwind/vitest and framework-free mrf/time/WebGL modules; added deterministic synthetic generator and initial tests. `pnpm install` completed, but the devenv pre-command dependency check then rejected esbuild's ignored build script. Added pnpm's narrow `onlyBuiltDependencies: [esbuild]` allowlist; next: reinstall and resolve compiler/test findings.
- 2026-08-28 14:59 UTC — Core/UI complete. Contract findings: no blocking ambiguity; Vite correctly answers exact Range requests with 206, while `MrfClient` also handles static servers that ignore Range and return 200. Synthetic set is deterministic: 85 frames (36 history, 25 nowcast, 24 harmonie) in 5 contract-v0 chunks, 376 KiB total, including dry frames. UX decisions: map-first dark layout; bottom meter/timeline on mobile and right rail on desktop; square-root graph scaling keeps drizzle visible next to showers; fractional slider cursor and shader blending; Dutch labels and graceful geolocation denial. Corrected shader UVs and added in-flight request deduplication during manual code review.
- 2026-08-28 15:00 UTC — Final synchronous receipts from `web/`: `pnpm synthgen` exit 0 (85 frames/5 chunks); `pnpm typecheck` exit 0; `pnpm test` exit 0 (2 files, 4 tests; synth chunk decode verified byte-exact by SHA-256 and time precedence/interpolation covered); `pnpm build` exit 0. Build warning only: MapLibre keeps the main minified chunk at 1,077 KiB (293 KiB gzip); acceptable for this shell, potential later split. Manual dev verification: `pnpm dev --host 127.0.0.1` reached Vite ready; fetched `/`, `/data/manifest.json`, and bytes 0–31 of a chunk; observed HTTP 200/200/206 respectively, correct `Content-Range`, and exactly 32 response bytes. This environment has no graphical browser, so visual interaction (rain animation, click marker, geolocation, responsive breakpoints) remains for orchestrator/PO browser verification. Next: commit, publish draft PR per repository policy.
- 2026-08-28 15:03 UTC — Committed implementation as `146d3f7`, pushed `track/t3-frontend-shell`, verified the remote ref with `git ls-remote`, and opened draft PR https://github.com/mathijshenquet/motregen/pull/1. First `gh pr create` attempt hit a transient API connection failure; retry succeeded. Track implementation is complete; only the explicitly documented graphical browser review remains external.
