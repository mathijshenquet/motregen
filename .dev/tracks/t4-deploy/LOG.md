# T4 deploy — werklog

- 2026-08-31 09:22 UTC — Track gestart vanaf `98b95bc`. Spec, `AGENTS.md`, MIP-6, MIP-3, Caddy-contract, serving/seamless-documentatie en ingest-CLI gelezen. Werkboom was schoon; `.dev/tracks/t4-deploy/LOG.md` bestond nog niet. Keuzes uit de spec: productie volgt `main`, Cloudflare komt direct voor de origin, secrets blijven uitsluitend in `/var/lib/motregen/secrets.env`, en lokaal NixOS-VM-bewijs vervangt installatie op de nog niet bestaande VPS. Volgende stap: flake/modulegrenzen en fixtures implementeren.
- 2026-08-31 09:35 UTC — Eerste complete implementatie neergezet: flake-output voor Rust/web, herbruikbare motregen- en hostmodules, disko GPT-layout (BIOS+ESP+ext4-root), zram, SSH/firewall, dagelijkse auto-upgrade met nachtelijk rebootvenster, wekelijkse GC, Caddy-productievhost, VM-fixtures/test en Nederlands deployrunbook. De VM vervangt alleen de netwerkafhankelijke ingest-uitvoerbare door een actieve stub; de echte ingest- en webderivaties zijn afzonderlijke checks. Ext4 gekozen voor minimale operationele complexiteit op een single-purpose box zonder snapshotbehoefte. Volgende stap: lockfile genereren, dependencyhash invullen en alle evaluatie-/buildfouten oplossen.
- 2026-08-31 09:43 UTC — Flake gelockt op nixpkgs `d2f6794` (2026-08-29) en disko `ff8702b`; `nix flake show --all-systems` evalueert alle outputs. Pnpm fixed-output-hash bepaald en ingevuld. Synchrone gerichte builds: `nix build .#motregen-web -L --no-link` exit 0; `nix build .#motregen-ingest -L --no-link` exit 0 inclusief 15 ingesttests en succesvolle ecCodes/HDF5/NetCDF-linking. Eerste `nix build .#checks.x86_64-linux.nixos-vm -L --no-link` bootte, activeerde zram, ingest en Caddy, maar faalde bij de eerste HTTP-assertie zonder de gevangen headers te tonen. Diagnostiek aan asserts toegevoegd; volgende stap: exacte Caddy-respons uitlezen en repareren.
- 2026-08-31 10:02 UTC — VM-fout herleid tot systemd `DynamicUser`: `StateDirectory=motregen` migreert de persistente data naar `/var/lib/private`, waardoor Caddy rechtstreeks op `/var/lib/motregen` een 403 gaf. De motregenmodule bind-mount de datamap nu read-only op `/run/motregen-data` in Caddy's mount namespace. Herhaalde synchrone `nix build .#checks.x86_64-linux.nixos-vm -L --no-link` eindigde met exit 0: ingest en Caddy actief, manifest 200 met exact MIP-3-cache/CORS/Range-headers, chunk Range-respons 206 met `bytes 0-7/44`, body `MRF0TEST` en frontend 200.
- 2026-08-31 10:10 UTC — OVH-host is beschikbaar als `vps-e26d083b.vps.ovh.net` (`57.129.47.17`, `2001:41d0:701:1100::d923`) en de gebruiker heeft sleuteltoegang voor tijdelijke gebruiker `ubuntu` bevestigd. Geen remote wijziging uitgevoerd: lokale VM-verificatie blijft de scope. Runbook ingevuld met de echte adressen, tijdelijke passwordless-sudo-voorwaarde en behoud van hostkeys. Declaratieve roottoegang bevat nu zowel de lokaal aanwezige `ageq-mthq`-sleutel als de door Mathijs aangeleverde MacBook Pro-sleutel. Firewall blijft default-deny met uitsluitend TCP 22/80/443 en UDP 443. Volgende stap: productieconfiguratie bouwen en alle volledige gates draaien.
- 2026-08-31 09:55 UTC — Correctie: de twee voorafgaande entries kregen per abuis toekomstige tijden `10:02` en `10:10`; hun werkelijke tijden vielen vóór 09:55 UTC. Eerste synchrone productiebuild `nix build .#nixosConfigurations.motregen.config.system.build.toplevel -L --no-link` eindigde met exit 1 nadat lokale DNS alle cache.nixos.org-lookups weigerde en een Nix-daemonworker crashte. Dit was geen configuratiefout: `nix store ping` reageert weer, de hoofd-daemon is actief en cache.nixos.org resolveert naar `146.75.121.91`. Volgende stap: dezelfde gate opnieuw draaien.
- 2026-08-31 10:00 UTC — Volledige productie-closure synchroon gebouwd met `nix build .#nixosConfigurations.motregen.config.system.build.toplevel -L --no-link --max-jobs 1 --option http-connections 1`, exit 0; Nix meldde eerlijk dat de onbetrouwde clientoptie `http-connections` genegeerd werd, terwijl `--max-jobs 1` de lokale resolverdruk voldoende beperkte. Build omvatte disko/bootloader/initrd, echte ingest (15 tests), frontend, Caddy/systemd en beide rootkeys. Hoofdgate `nix flake check -L` daarna exit 0 met drie checks, inclusief de reeds gebouwde VM-test. Workspacegate `direnv exec . bash -c 'cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace'` exit 0; frontendgate `direnv exec . bash -c 'cd web && pnpm typecheck && pnpm test && pnpm build'` exit 0 (18 bestanden/55 tests; alleen de bestaande MapLibre-chunkwaarschuwing). Rust-Nix-bronset vervolgens beperkt tot `Cargo.toml`, `Cargo.lock` en `crates/`, zodat dit append-only log de packagehash niet langer wijzigt. Volgende stap: definitieve `nix flake check -L` op de volledige diff, dan commit en draft-PR.
- 2026-08-31 10:14 UTC — Definitieve hoofdgate werd eerst tweemaal extern geblokkeerd: `nix flake check -L` en daarna de gerichte ingestbuild eindigden exit 1 omdat de enige lokale DNS-upstreams `185.12.64.2/.1` time-outten. Op verzoek van Mathijs is `ageq-mthq` buiten deze track declaratief omgezet naar Cloudflare `1.1.1.1/.0.0.1`, met behoud van Tailscale MagicDNS; NixOS-switch exit 0, publieke namen, OVH-DNS en MagicDNS-peers resolven, en die aparte nix-configwijziging staat in commit `292a75e`. Vervolgens exact `nix flake check -L` synchroon exit 0: alle drie checks groen, de nieuwe beperkte-source ingestderivatie volledig gebouwd met 15 tests, webcheck aanwezig en VM-testoutput aanwezig. Volgende stap: finale productie-closure op deze derivatie bouwen, snelle hoofdgate herhalen, diff reviewen en commit/draft-PR publiceren.
- 2026-08-31 10:15 UTC — Finale productieconfiguratie synchroon gebouwd met exact `nix build .#nixosConfigurations.motregen.config.system.build.toplevel -L --no-link`, exit 0; resultaat `nixos-system-motregen-26.11.20260829.d2f6794` gebruikt de definitieve ingestderivatie. Volgende stap: laatste `nix flake check -L`, commit en publiceren.
- 2026-08-31 10:16 UTC — Laatste hoofdgate op de uiteindelijke loginhoud: exact `nix flake check -L`, synchroon exit 0 met packages, modules, productieconfiguratie en drie checks inclusief `nixos-vm`. `git diff --cached --check` schoon; staged scope bestaat uitsluitend uit de 15 T4-bestanden, twee publieke SSH-sleutels en een expliciet fake VM-secret, zonder echte KNMI-key. Implementatie en lokaal bewijs compleet; volgende stap: commit, push en draft-PR.
- 2026-08-31 10:19 UTC — Implementatie gecommit als `eef931f44732203610c06af5fae65fb22a043197`, branch gepubliceerd en draft-PR #15 geopend: https://github.com/mathijshenquet/motregen/pull/15. Remote `track/t4-deploy` wees synchroon naar exact `eef931f`; PR beschrijft alle lokale receipts en vermeldt expliciet dat de echte VPS niet is gewijzigd. Volgende stap: deze laatste logreceipt committen/pushen en remote/PR-head finaal verifiëren.
- 2026-08-31 10:22 UTC — Logreceiptcommit `3e79afb600df8d8fabd78ca4b56d2c8e830144db` gepubliceerd; synchrone controles bevestigden dat remote branch en draft-PR #15 beide exact naar `3e79afb` wijzen, PR OPEN+DRAFT tegen `main`, zonder geconfigureerde checks. De afzonderlijke actieve DNS-fix is als `292a75e5bbaef87c37e913041249425debd4fd4b` naar `nix-config/main` gepusht. T4 is lokaal bewezen en overdrachtsklaar; daadwerkelijke VPS-installatie blijft onuitgevoerd.
- 2026-08-31 10:29 UTC — Mathijs gaf expliciet toestemming voor de destructieve installatie op de lege OVH-VPS. Read-only preflight via `ubuntu@57.129.47.17`: passwordless sudo beschikbaar; BIOS-boot; `/dev/sda` is exact 40 GB QEMU-disk met Ubuntu; NIC `ens3` heeft MAC `fa:16:3e:c7:02:3f`; IPv4 `57.129.47.17/32` komt via DHCP; IPv6 `2001:41d0:701:1100::d923/128` gebruikt de statische on-link-gateway `2001:41d0:701:1100::1`. Eén SSH-poging time-outte vóór een succesvolle retry; niets was toen gewijzigd. Productieconfiguratie aangevuld met systemd-networkd voor exact deze OVH-topologie. Volgende stap: closure/gegenereerde network-unit en volledige flake opnieuw lokaal bewijzen, commit/push, dan nixos-anywhere starten.
- 2026-08-31 10:31 UTC — Hostspecifieke netwerkconfig lokaal bewezen. `nix build .#nixosConfigurations.motregen.config.system.build.toplevel -L --no-link --print-out-paths` exit 0 en gegenereerde `10-ovh-uplink.network` handmatig gecontroleerd: exacte MAC, DHCPv4, `UseDNS=false`, OVH+Cloudflare DNS, statisch IPv6 `/128`, link-scope `/64` en `GatewayOnLink=true` voor de IPv6-default. Daarna exact `nix flake check -L` synchroon exit 0 met alle drie checks. `git diff --check` schoon. Volgende stap: commit/push en destructieve nixos-anywhere-run.
- 2026-08-31 10:37 UTC — OVH-netwerkcommit `dc50ef6` naar `track/t4-deploy` gepusht. Eerste geautoriseerde installatiepoging met `nix run github:nix-community/nixos-anywhere -- --copy-host-keys --flake .#motregen ubuntu@57.129.47.17` stopte synchroon met exit 1 tijdens het laden van de kexec-kernel: Ubuntu 26.04 koos via `--kexec-syscall-auto` voor `kexec_file_load`, dat `Address not available` retourneerde. Veiligheidscontrole erna bevestigde dat geen kexec-kernel geladen is (`/sys/kernel/kexec_loaded=0`) en Ubuntu plus de originele `/dev/sda`-partities nog intact zijn; disko is niet gestart. Volgende stap: de door nixos-anywhere ondersteunde legacy-kexec-syscall afdwingen en de installatie hervatten.
- 2026-08-31 10:45 UTC — Tweede geautoriseerde installatiepoging met `--kexec-extra-flags '--kexec-syscall'` eindigde synchroon met exit 0 (`### Done! ###`): legacy kexec werkte, disko verving `/dev/sda`, NixOS installeerde GRUB voor BIOS én EFI en rebootte. Post-boot via strict hostkey checking: NixOS 26.11, hostname `motregen`, kernel 6.18.48, ext4-root `/dev/sda3` en exact productiepad `38zb7h…-nixos-system-motregen-26.11.20260829.d2f6794`. IPv4/IPv6, beide default-routes en OVH+Cloudflare-resolvers zijn correct; de bestaande ED25519-hostkey bleef behouden. Beide gecommitteerde rootkeys werken declaratief en password-only root-SSH wordt geweigerd. Externe nmap-receipt: TCP 22/80/443 open; 21/23/25/53/445/3000/8080 filtered. Caddy is actief en de origin geeft via geforceerde Host-resolutie HTTP 308; publiek DNS wijst nog naar parkeer-A-records `207.207.210.229/.107` en heeft geen AAAA, waardoor ACME nog terecht faalt. De lokaal genegeerde `.env` bevat geen `KNMI_OPEN_DATA_API_KEY`; `secrets.env` is daarom bewust niet aangemaakt en ingest blijft tot veilige key-aanlevering in de voorgeschreven restart/backoff. Runbook bijgewerkt met de op deze OVH-kernel noodzakelijke legacy-kexec-vlag. Volgende stap: finale lokale flake-check, commit/push; daarna moet Mathijs secret plaatsen en DNS/Cloudflare omzetten.
- 2026-08-31 10:47 UTC — Finale hoofdgate na runbook/logwijzigingen: exact `nix flake check -L`, synchroon exit 0 met packages, modules, productieconfiguratie en drie checks inclusief de NixOS-VM-test. Laatste VPS-receipt: `firewall`, `systemd-networkd`, `sshd` en `caddy` allemaal active; nul failed units; `nixos-upgrade.timer` gepland voor 2026-09-01 03:19 CEST en `nix-gc.timer` voor 2026-09-06 04:30 CEST. Deploy technisch voltooid; alleen het bewust niet beschikbare secret en de externe DNS/Cloudflare-omzetting resteren.
- 2026-08-31 10:49 UTC — Deploydocumentatie en volledige installatie-/verificatiereceipts gecommit als `a8a6442564e8fe6926da4a72041acc11298bec20` en gepubliceerd. `git ls-remote` en draft-PR #15 bevestigden na korte GitHub-consistentievertraging exact die head; PR blijft OPEN+DRAFT tegen `main`: https://github.com/mathijshenquet/motregen/pull/15. Werkboom was daarna schoon. Trackoverdracht compleet; resterende secret- en DNS-acties vereisen invoer respectievelijk dashboardtoegang van Mathijs.
- 2026-08-31 12:58 UTC — Mathijs plaatste de productiecredentials in de genegeerde worktree-`.env`. Alleen de door ingest ondersteunde `KNMI_OPEN_DATA_API_KEY` is via stdin atomisch als root-owned mode `0600` naar `/var/lib/motregen/secrets.env` gestuurd; de aanwezige notificatie-, Porkbun- en Cloudflare-secrets zijn bewust niet naar de VPS gekopieerd. Ingest startte direct active/running met nul restarts en voltooide de eerste backfill: RTCOR 36 frames, nowcast 25 frames, seamless 48 frames/20 ensembleleden, 25 AROME-leads en uiteindelijk een manifest van 13.458 bytes. Productieverificatie vond vervolgens een echte seam: `tempfile::NamedTempFile` negeert voor zijn veilige `0600`-default de service-UMask, zodat manifest en chunks in Caddy's mount namespace niet leesbaar waren. Publisher zet publieke tempfiles nu vóór de atomische rename expliciet op `0644` en repareert reeds bestaande, inhoudelijk identieke chunks; `secrets.env` valt buiten die paden en blijft `0600`. Nieuwe regressietest bewijst beide gevallen. Gerichte publishertests exit 0; volledige `cargo fmt`/Clippy-als-fout/workspacetests exit 0; exact `nix flake check -L` synchroon exit 0 inclusief nieuwe releasebuild (16 ingesttests) en NixOS-VM-check. Volgende stap: commit/push, productieclosure switchen, permissies en HTTP-contract live verifiëren.
- 2026-08-31 13:08 UTC — Publieke-datafix gecommit/gepusht als `60a3a65d54d698326b610436422f6cefac546f79`. Bestaande manifest/chunks eenmalig beperkt tot exact die publieke paden naar `0644` hersteld; Caddy-user kon daarna lezen en `secrets.env` bleef `0600`. Synchrone `nixos-rebuild switch --flake .#motregen --target-host root@57.129.47.17 --build-host localhost -L` eindigde exit 0 en activeerde productieclosure `mjcqr58…` met nieuwe ingestbinary `09p2n8…`; ingest begon een nieuwe cache-gevoede backfill. Finale live-receipt: ingest/Caddy/firewall active, nul failed units, PID 2261 met nul restarts, manifest `0644`/13.458 bytes, secret `0600`. Het manifest is daadwerkelijk via Caddy's mount namespace als Caddy-user gelezen en lokaal met `jq -e` gevalideerd: versie 0, gegenereerd 12:55:45Z, now 12:20Z, 12 chunks, bronnen RTCOR/nowcast/seamless/HARMONIE/UV en negen verwachte velden. Secretplaatsing, eerste publicatie en productie-read-seam zijn volledig afgehandeld; DNS/ACME blijft de afzonderlijke nog openstaande externe stap.
- 2026-08-31 13:12 UTC — Tijdens PR-statusupdate bleek oorspronkelijke draft-PR #15 extern gemerged op oude head `1521672`; deze agent voerde geen merge uit. `origin/main` op mergecommit `7e81b7d` bevat de oorspronkelijke deploy, maar nog niet de daarna live gevonden permissiefix. `git log origin/main..origin/track/t4-deploy` en triple-dot diff bevestigden exact twee ontbrekende commits/twee bestanden. Daarom draft-PR #17 geopend tegen `main`, exact op head `e65378078dca7ff653ffe5ba410e846abd6d2714`: https://github.com/mathijshenquet/motregen/pull/17. De PR beschrijft oorzaak, secret-isolatie, alle lokale gates en live productieproef; geen merge uitgevoerd.
- 2026-08-31 15:15 UTC — Op verzoek de live sessiegrootte vóór T2g exact uit het actuele manifest gemeten: 12 gerefereerde chunks samen `19.407.366` bytes (18,51 MiB), waarvan acht HARMONIE-uurveldchunks respectievelijk `1.641.685`, `1.028.180`, `1.028.428`, `1.534.203`, `1.310.085`, `1.334.019`, `2.365.477` en `2.640.011` bytes. De hele retained chunkdirectory was 32 bestanden/`62.902.621` bytes; voor een eerlijke voor/na-vergelijking telt alleen de door het manifest gerefereerde sessie. Het manifest was `0644`, 13.746 bytes, gegenereerd om `15:10:13Z`, en de actieve productieclosure bleef `mjcqr58…`. Eerste SSH-triggerpoging time-outte vóór verbinding; na 5 seconden backoff startte `systemctl start nixos-upgrade.service` wel en werd dezelfde SSH-sessie synchroon afgewacht. De unit eindigde na circa 1m50s met exit 1: hij haalde de gevraagde laatste `main` (`4d27ad2`, T2g) op, maar de webderivatie had nog de oude `fetchPnpmDeps`-hash (`b942…`; werkelijk `IM8M…`). Geen switch vond plaats; ingest en Caddy bleven active op de oude closure. Een eerste diagnostische reconnect time-outte eveneens en de retry na 5 seconden werkte. De nachtelijke 03:19-run zou op dezelfde deterministische hashfout stranden zolang `main` niet wordt gecorrigeerd.
- 2026-08-31 15:22 UTC — `origin/main` in de bestaande follow-upbranch gemerged als `26df07a`; alleen `nix/packages/web.nix` hoefde inhoudelijk te worden gecorrigeerd naar de door Nix gemelde pnpm-dependencyhash `sha256-IM8MNOGmTCKPLZm9HcrOjqagQDQEYohzmZyD3egaqc4=`. Eerste gerichte webbuild werd extern door een tijdelijke `cache.nixos.org`-DNS-fout gestopt; onmiddellijke herhaling van exact `nix build .#motregen-web -L --no-link` eindigde exit 0. Daarna eindigde exact `nix flake check -L` synchroon exit 0: nieuwe ingest-release met 20 tests, webderivatie en de volledige NixOS-VM-test groen; in de VM waren ingest/Caddy actief, manifest-respons 200, chunk-range 206 en frontend-respons 200. Volgende stap: hashfix/log committen en PR #17 bijwerken. Een nieuwe productie-upgrade en de gevraagde na-meting kunnen pas exact via `main` worden uitgevoerd nadat deze fix (plus de nog ontbrekende live permissiefix in dezelfde PR) daar expliciet is gemerged; deze agent merge niet zonder toestemming.
- 2026-08-31 15:24 UTC — Hashcorrectie en bovenstaande receipts gecommit als `e82adc23197c31a2b43416724baec8f6f3cca19e` en synchroon naar `origin/track/t4-deploy` gepusht. Draft-PR #17 bijgewerkt naar “Fix production deploy regressions” met de exacte upgradefout, voor-meting, SSH-time-outs en lokale VM-receipt: https://github.com/mathijshenquet/motregen/pull/17. `git ls-remote` en GitHub bevestigden beide exact head `e82adc2`, OPEN+DRAFT tegen `main`; werkboom schoon. Volgende stap vereist expliciete merge naar `main`, daarna opnieuw `systemctl start nixos-upgrade.service` synchroon afwachten en de actieve services, nieuw manifest en gerefereerde chunkbytes na T2g meten.
- 2026-08-31 15:27 UTC — Read-only live eindcontrole met oplopende SSH-backoff: pogingen 1 en 2 time-outten op poort 22, poging 3 verbond wel en bevestigde `motregen-ingest`, `caddy` en `firewall` alle drie active; `/run/current-system` wees nog exact naar de ongewijzigde oude closure `mjcqr58…`. De aanvullende manifestprobe gebruikte eerst het niet-canonieke DynamicUser-pad en vond daar terecht niets; een gecorrigeerde reconnect time-outte opnieuw. Geen remote wijziging uitgevoerd. De eerder synchroon verkregen manifest- en byte-receipts blijven de geldige voormeting; een na-meting bestaat nog niet omdat de upgrade aantoonbaar niet geswitcht is.
