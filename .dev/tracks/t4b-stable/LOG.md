# T4b stable — werklog

- 2026-08-31 16:20 UTC — Track gestart op nieuwe branch `track/t4b-stable`, exact vanaf `origin/main` commit `d5756ff9485b47824dcc19ade7baa31f0822b9fc`. MIP-6 §5 gelezen: nixpkgs moet naar `github:NixOS/nixpkgs/nixos-26.05`; auto-upgrade blijft `main`; geen VPS-switch in deze track vóór onafhankelijke verificatie en merge. Werkboom was schoon en remote branch bestond nog niet. Volgende stap: huidige flake/lock en devenv inspecteren, input wijzigen en uitsluitend nixpkgs herlocken.
- 2026-08-31 16:22 UTC — Devenv/direnv was actief en toegestaan. `flake.nix` omgezet van `nixos-unstable` naar exact `github:NixOS/nixpkgs/nixos-26.05`; `nix flake update nixpkgs` eindigde synchroon met exit 0 en wijzigde uitsluitend de gevolgde nixpkgs-node van `d2f6794` naar stable commit `c5c4a43` (beide 2026-08-29). Disko blijft dezelfde nixpkgs-input volgen. Volgende stap: flake evalueren en ingest/web gericht bouwen om versieverschillen vroeg te isoleren.
- 2026-08-31 16:26 UTC — `nix flake show --all-systems` evalueerde alle packages, modules, productieconfiguratie en checks succesvol op stable. Gecombineerde gerichte `nix build .#motregen-web .#motregen-ingest -L --no-link` eindigde synchroon exit 1: web bereikte met stable `pnpm 10.34.0` de configurefase, maar ingest stopte vóór compilatie omdat stable uitsluitend Rust 1.95 levert en alle workspacecrates expliciet `rust-version = 1.97` vereisen. Stable attrs bevatten alleen `rustPackages_1_95`; de actieve devenv gebruikt aantoonbaar Rust 1.97.1/cargo 1.97.0. Besluit: MSRV niet kunstmatig verlagen; een expliciet gepinde Rust 1.97-toolchain aan de verder stable package-set koppelen en daarna beide builds herhalen.
- 2026-08-31 16:29 UTC — `rust-overlay` toegevoegd als gelockte input die stable nixpkgs volgt (commit `d03cd47`, 2026-08-31); `pkgs.rust-bin.stable."1.97.0".minimal` voedt via `pkgs.makeRustPlatform` uitsluitend de ingestderivatie. Er is dus geen tweede unstable-nixpkgs-set en NixOS/alle systeempackages blijven op 26.05. Herhaling van exact `nix build .#motregen-web .#motregen-ingest -L --no-link` eindigde synchroon exit 0: web bouwde met pnpm 10.34.0 zonder hashwijziging; ingest bouwde/linkte met Rust/cargo 1.97.0 en stable ecCodes 2.47.0, HDF5 1.14.6 en NetCDF 4.9.3; 20 ingesttests groen. Volgende stap: release-identiteit controleren en de volledige flake-/VM-, workspace- en webgates draaien.
- 2026-08-31 16:39 UTC — Release-evaluatie gaf exact `26.05` / `26.05.20260829.c5c4a43`. Hoofdgate `nix flake check -L` eindigde synchroon exit 0: alle outputs evalueerden, ingest/webchecks waren gebouwd en de volledige stable NixOS-VM bootte met kernel 6.18.48/systemd 260; ingest en Caddy actief, manifest HTTP 200 met cache/CORS/Range-contract, chunk-range 206 met `bytes 0-7/44` en body `MRF0TEST`, frontend HTTP 200. Workspacegate `direnv exec . bash -c 'cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace'` eindigde synchroon exit 0. Webgate `direnv exec . bash -c 'cd web && pnpm typecheck && pnpm test && pnpm build'` eindigde synchroon exit 0: 19 bestanden/58 tests groen en productiebuild geslaagd; alleen de bestaande chunk-sizewaarschuwing. Volgende stap: ook de echte productie-toplevelclosure bouwen, dan diff reviewen en commit/push.
- 2026-08-31 16:41 UTC — Exact `nix build .#nixosConfigurations.motregen.config.system.build.toplevel -L --no-link --print-out-paths` eindigde synchroon exit 0 en bouwde de volledige OVH-productieclosure `/nix/store/qx5rrw100994iqbam1qzgdgq8h1wfdzv-nixos-system-motregen-26.05.20260829.c5c4a43`, inclusief disko/bootloader, hostnetworkd, auto-upgrade, Caddy en echte ingest. Geen VPS-commando of switch uitgevoerd. Volgende stap: lock/diff/format reviewen, committen, nieuwe remote branch pushen en remote head verifiëren.
- 2026-08-31 16:42 UTC — Tijdens de eindreview bleek `origin/main` na de branchstart één documentatiecommit te zijn doorgeschoven (`6f0fc66`, geaccepteerde MIP-8/T3h-spec; geen uitvoerbare code). `git rebase --autostash origin/main` eindigde synchroon exit 0; HEAD en merge-base zijn nu beide exact `6f0fc66757bf77b583338f30fbd03bde939f0976`, zonder conflicten. `git diff --check` eindigde synchroon exit 0; de lock bevat één stable nixpkgs-node en zowel disko als rust-overlay volgen diezelfde node. In de actieve omgeving is geen Nix-formatter beschikbaar; de kleine handmatige wijziging volgt de bestaande flakestijl. Volgende stap: commit, push, draft-PR en remote-headreceipt.
- 2026-08-31 16:43 UTC — Alle gevraagde gates nogmaals op de definitieve `origin/main`-basis uitgevoerd. `nix flake check -L` eindigde synchroon exit 0 en valideerde packages, modules, productieconfiguratie en de drie checks inclusief de reeds gebouwde volledige VM-test. `direnv exec . bash -c 'cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace'` werd synchroon uitgewacht en eindigde exit 0. `direnv exec . bash -c 'cd web && pnpm typecheck && pnpm test && pnpm build'` eindigde synchroon exit 0 (19 testbestanden/58 tests en Vite-build; uitsluitend de bekende niet-fatale chunk-sizewaarschuwing). Werkboom bevat uitsluitend `flake.nix`, `flake.lock` en dit tracklog. Volgende stap: commit/push/PR en remote verificatie.
- 2026-08-31 16:52 UTC — Implementatie en eerdere receipts gecommit als `b8121c3847710d3d28c90f5f26819357eef6e76d` (`Pin deployment to NixOS 26.05`). De eerste HTTPS-push was stil/traag; begrensde herhaling werd synchroon uitgewacht en eindigde exit 0 (`Everything up-to-date`), waarna `git ls-remote --heads origin refs/heads/track/t4b-stable` exact dezelfde commit teruggaf. GitHub GraphQL time-outte herhaaldelijk terwijl git en REST bereikbaar waren; daarom de draft-PR via GitHubs officiële REST-endpoint aangemaakt en de response gecontroleerd: PR #21, open/draft, base `main`, head `track/t4b-stable`, URL `https://github.com/mathijshenquet/motregen/pull/21`. Geen VPS-switch uitgevoerd. Volgende stap: dit slotreceipt committen/pushen en remote PR-head plus schone werkboom controleren.
- 2026-08-31 16:58 UTC — Bij de remote-readback bleek `origin/main` tijdens publicatie opnieuw te zijn doorgeschoven, nu naar T3g-merge `b114f3c4acaa9337ad1cb8d95b6c444b7d4d9f57` met substantiële webwijzigingen. Om stable ook tegen het echte PR-merge-resultaat te bewijzen, beide trackcommits conflictloos op die commit gerebased; merge-base is exact `b114f3c`. Alle gates daarna opnieuw vanaf nul waar de bronwijziging dat vereiste: `nix flake check -L` eindigde synchroon exit 0 en bouwde/bootte een nieuwe stable VM, met ingest+Caddy actief, manifest/frontend HTTP 200 en chunk-range 206 (`bytes 0-7/44`, `MRF0TEST`); workspace fmt+clippy+tests eindigden synchroon exit 0; web typecheck+tests+build eindigden synchroon exit 0 met de nieuwe suite (22 bestanden/73 tests, alleen bekende chunk-sizewaarschuwing). Productieclosure `nix build .#nixosConfigurations.motregen.config.system.build.toplevel -L --no-link --print-out-paths` eindigde synchroon exit 0 op `/nix/store/dr60h3shvw02x3ps8bj1fh4053aqq7mn-nixos-system-motregen-26.05.20260829.c5c4a43`. Geen VPS-commando of switch uitgevoerd. Volgende stap: log committen en de gerebasede PR-head uitsluitend met expliciete force-with-lease op bevestigde remote commit `8c2c16c8328d362561e8c51f9dba892a2e3ec8d7` publiceren.
- 2026-08-31 17:04 UTC — Slotpublicatie voltooid. Vlak vóór herschrijven gaf `git ls-remote` nog exact de verwachte lease `8c2c16c8328d362561e8c51f9dba892a2e3ec8d7`; `git push --force-with-lease=refs/heads/track/t4b-stable:8c2c16c…` eindigde synchroon exit 0 en publiceerde de op nieuwste main gerebasede head `564f64df2ad772d67d7138d1c0f4129264a52f80`. Na begrensde retry bevestigde GitHub REST PR #21 als open/draft, base `main`, head `track/t4b-stable`, head-SHA exact `564f64d`, en de bijgewerkte 22-bestanden/73-tests-verificatie. Laatste gecombineerde read-only check eindigde exit 0: remote main exact `b114f3c`, remote branch exact `564f64d`, lokale HEAD gelijk aan remote branch, merge-base gelijk aan remote main en een schone werkboom. Trackinhoud tegenover main: uitsluitend `flake.nix`, `flake.lock` en dit committed LOG. De VPS is onaangeraakt gebleven. Track gereed voor PO-verificatie; na deze logcommit resteert alleen een normale push en read-only refcheck.
