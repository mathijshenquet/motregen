# Track U20 — windregressies na U12/U19

- 2026-09-24 13:35 UTC — Start (claude opus 5.5, worktree `track/u20-wind-regressie` op main `ab29af0`). Spec, `wind-layer.ts`, `focus-mode.ts`, App-bedrading en U12-LOG gelezen. Bisectpunten: vóór U12 = `c623612^1`, ná U12 = `c623612`, ná U19 = main.
- 2026-09-24 13:35 UTC — Code-observaties vóór meten: (1) `storeWindTuning` schrijft de hele tuning weg zodra één knop afwijkt; een PO die ooit een knop verschoof heeft dus `intensity: 1.9` (oude default, U3b c6118e9) vast in `motregen-wind-tuning-v2` en U19's 1,27 bereikt hem nooit; windfocus maakt daar ×1,5 = 2,85 van. (2) `focusedWindTuning` → `setTuning` → `resetViewport()` op elke tweenframe van de focus. (3) U12 zette de bufferretentie bij uitzoomen op 1/schaal (was 1/schaal²) en blit de buffer bij resize.
- 2026-09-24 13:40 UTC — PO-queue (punt 5): op `?dev` een knop "Reset alle instellingen" die alle tuning-/debugkeys in localStorage wist en de app-state direct op defaults zet zonder reload (wind v2, isolijnen, labels, splash-slowdown, histogram-toggle, focus-pins); favorieten, laatste locatie/kaartview en thema blijven. Toast "Standaardwaarden hersteld". Per-paneel-reset van wind blijft. Unit test: tuningkeys weg, favorieten/locatie intact. Na de windfix.
- 2026-09-24 13:40 UTC — Tooling: worktree had geen node_modules (`pnpm install --frozen-lockfile --prefer-offline`), `.envrc` stond geblokkeerd (`direnv allow`). Drie builds (`git archive` naar scratch, `vite build --outDir`), previews :4461 pre-U12, :4462 U12, :4463 main, alle met `MOTREGEN_DATA_ORIGIN=https://motregen.nl/data`. Nieuw meetscript `web/scripts/wind-bisect.ts`.
- 2026-09-24 13:55 UTC — Meetmethode (`web/scripts/wind-bisect.ts`, na twee mislukte opzetten): 1280×800 DPR 2 (kaartdeel ~810×800 CSS-px), live data via `MOTREGEN_DATA_ORIGIN=https://motregen.nl/data`, start 52,35/4,9 (kaart fit op z6,79), autoplay gepauzeerd, adaptief budget uit (zoals `wind-zoom.ts`; anders schaalt swiftshader op DPR 2 zijn eigen particles terug — eerste run had daardoor onvergelijkbare aantallen). Stills in rust en ~0,3/2,5/4,5 s na `moveend` van easeTo z6,79→z8 (1,2 s) en terug; bij elke still direct een tweede screenshot met alleen `.map-overlay-motregen-wind` verborgen = basis (labels/tegels laden na zoom na en telden in de eerste opzet als inkt mee). Inkt = gem. |ΔRGB| per land-/zeepixel (wind-ink-methode), "strong" = aandeel pixels met |ΔRGB| > 24. Een vast 0/2/4/6/8 s-schema bleek onbruikbaar: DPR 2-screenshots kosten >1 s onder swiftshader, stills vielen midden in de zoom. Run onder `flock`, load-poll < 16: `flock /tmp/motregen-e2e.lock direnv exec .. $S/run-bisect.sh` → BISECT-EXIT 0. Data: `bisect/{leeg,po}-{pre,u12,main}.json`, `bisect/bisect-run.txt`. "po" = volledige v2-tuning met de U3b/U12-defaults (intensity 1,9), zoals v2 hem wegschreef zodra één knop afweek.
- 2026-09-24 13:55 UTC — Bisect-uitkomst (zee-inkt, rust → 0,3 s na inzoomen; "strong" idem), leeg / licht+donker:
  | build | licht zee | licht strong | donker zee | donker strong | na uitzoomen 0,3 s |
  |---|---|---|---|---|---|
  | pre-U12 `c623612^1` | 1,83 → 0,55 | 0,012 → 0,004 | 2,50 → 0,79 | 0,013 → 0,004 | ~0,25× (gat) |
  | U12 `c623612` | 1,98 → 2,77 | 0,013 → 0,016 | 2,48 → 5,39 | 0,012 → **0,046** | ~1× |
  | main (U19) | 1,34 → **6,21** | 0,011 → **0,042** | 1,74 → 2,23 | 0,012 → 0,012 | ~1× |
  | main, PO-tuning | 1,90 → **6,71** | 0,012 → **0,042** | 2,68 → 5,33 | 0,014 → **0,048** | ~1× |
  Conclusies: (1) pre-U12 heeft het oude zoomgat (inkt ~25–30 % direct na elke zoom), U12 lost dat op; (2) **U12 introduceert een flits direct na inzoomen**: 1,2–4,6× de rustinkt en 3–4× zoveel harde pixels, varieert sterk per run; de stills tonen een regelmatig raster van gelijke streepjes over het hele beeld (`main-light-1-in-0.3s`), boven land het duidelijkst omdat daar weinig andere inkt is = de "artefacten boven land"; (3) U19 werkt alleen zonder opgeslagen tuning (rust ×0,67–0,70); **met de v2-tuning die de PO vrijwel zeker heeft blijft main op 1,9** (laag `intensity: 1.9`) en windfocus maakt daar 2,85 van — samen met (2) "heftiger i.p.v. minder heftig". Uitzoomen is in alle builds ná U12 in orde (geen gat, geen dubbeling).
- 2026-09-24 13:55 UTC — Oorzaak raster/flits (verdachte c, maar anders dan gedacht): aanvullers krijgen `ages = 1e-3` en een gedeelde `maxAge` (6 s). Boven land (zwakke wind, 1 m/s ≈ 7,5 px/s, 90 px afstand = 12 s) is maxAge de doodsoorzaak, dus het hele zoomcohort sterft en respawnt in hetzelfde kwart seconde, en blijft daarna synchroon (ook het koudestartcohort: stagger 0–2 s blijft een puls). Probe in vitest (nepkaart, 1 m/s): doden per 0,25 s, max/gemiddelde = 5,6 in rust en **19,4 na één zoom-in** (357 van ~400 particles in één venster); bij 5 m/s 1,2/1,8. Verdachte (b) buffer-blit: alleen bij resize, niet bij zoom; de fadepass warpt bij zoom zoals vóór U12 — geen dubbeling gemeten bij uitzoomen. Verdachte (d) halve pixel: niet waargenomen in de stills. Verdachte (a): bevestigd voor opgeslagen tuning; de focus-tween zelf eindigt correct op ×1,5 (bestaande unit + e2e).
- 2026-09-24 13:55 UTC — Fix 1 (commit volgt): per-particle levensschaal 0,8–1,2 op afstand én maxAge (was alleen afstand), en aanvullers krijgen dezelfde willekeurige fase in leeftijd als in afstand. Probe: max/gemiddelde rust 5,6 → 1,6, na zoom-in 19,4 → 2,1. Regressietest in `wind-layer-viewport.test.ts` (< 3; faalde vóór de fix op 5,6). Fix 2 (eerder, 04c7633): tuning v3 met alleen afwijkingen, v2 eenmalig gemigreerd — waarden gelijk aan een v2-default (intensity 0,5/1,4/1,9, lineWidth 1,5) volgen de huidige default; een zelfgekozen v2-intensiteit ×⅔ (zelfde verhouding als U19, zodat windfocus exact de oude waarde teruggeeft). Motivatie: v2 schreef de hele tuning weg zodra één knop afweek, dus "1,9" in opslag is vrijwel altijd de oude default en niet een keuze. **Vraag PO**: stuur "Kopieer als JSON" uit het perf-paneel (`?perf=1`) van je eigen browser, dan controleer ik de migratie op jouw echte opslag.
- 2026-09-24 14:00 UTC — **Correctie** op de 13:55-conclusie (2): de 3–5× inkt "direct na inzoomen" is grotendeels een **meetartefact**. Het trad alleen op bij de éérste zoom naar z8 na laden (ook in fix 1: ronde 1 5,2×, rondes 2–3 1,2×) en de bijbehorende stills tonen juist wéinig inkt (korte stompjes). Oorzaak: tegels/labels laden tussen still en basisbeeld nog na. Meetscript aangepast: opwarmronde (één zoom in/uit zonder opnames) en per still basis vóór én na; die moeten byte-gelijk zijn, anders opnieuw (`retries` in de JSON). Wat wél echt is en in de stills te zien: direct na inzoomen staan de aanvullers als **korte, even lange stompjes zonder staart op een regelmatig rooster** (leegste cel, jitter 0,6, allemaal in hetzelfde frame zichtbaar) — boven land het opvallendst, en dat is het artefact. Plus het maxAge-cohort (13:55, probe) dat daarna synchroon blijft pulseren.
- 2026-09-24 14:00 UTC — Fix 3 (4d05e7b): aanvullers krijgen volle jitter binnen hun cel en verschijnen gespreid over 0,15 s (ramp start negatief); 0,3 s liet de U12-pinchtest zakken (440 < 452 zichtbare koppen), 0,15 s niet. Unit: "fills of one zoom step appear spread out" (faalt zonder spreiding: 398 van ~430 in één frame). En `setTuning` doet alleen nog `resetViewport()` als de dichtheid verandert — de focus-tween zette elke frame een nieuwe tuning en liet daarmee elke frame de hele viewport-herverdeling lopen. Gates op 4d05e7b: `pnpm typecheck` exit 0, `pnpm test` exit 0 (232 tests), `pnpm build` exit 0. (Eerste `pnpm test` in deze verse worktree faalde op ontbrekende `public/data`; `pnpm synthgen` eerst.)
- 2026-09-24 14:12 UTC — Vergelijking main vs na (`cmp2`, fix2-build = 4d05e7b zonder de setTuning-opruiming; opwarmronde, stabiele basis, 2 zoomrondes, licht+donker): stills vallen nu ~1,8–2 s na `moveend` (drie screenshots per still onder swiftshader), dus het allereerste moment na de zoom zit alleen in de video's. Inkt ná ≥ 1,8 s: main en na gelijk (in: zee 1,5–2,4, uit: 1,0–1,8; geen gat, geen dubbeling). Op contactsheet-schaal (4 fps, ⅓) is het verschil subtiel; het harde bewijs voor fix 1/3 zijn de unit-probes (cohort 19,4 → 2,1; 398 → < 40 % aanvullers in één frame). Data `bisect/cmp2-{main,na}.json`; video's `video/{main,na}-{light,dark}.webm` (VP9 crf 45, 854 breed) + `-sheet.png` (4 fps rond de meet-inzoom).
- 2026-09-24 14:12 UTC — Gates op 4d05e7b (29e3030 = alleen LOG): `pnpm typecheck` exit 0; `pnpm test` exit 0 (41 bestanden, 232 tests); `pnpm build` exit 0; volledige e2e na load-poll (< 16) onder flock, poorten vooraf vrij: `MOTREGEN_E2E_PORT=4356 MOTREGEN_E2E_DATA_PORT=8356 direnv exec .. pnpm e2e` → **FULL-GATE-EXIT: 0** (29 passed, 25 skipped, 6,7 min), waaronder de nieuwe `wind-zoom.spec.ts` (desktop, 43,5 s). NIET gedaan: de e2e laten falen tegen de pre-U12-windlaag (alleen de unit-tests zijn tegen oude code geverifieerd).

## Slot U20

Per symptoom — veroorzaker → fix:

| symptoom PO | veroorzaakt door | wat er gebeurt | fix |
|---|---|---|---|
| (1) zoom-fade | vóór U12 (`c623612^1`): het oude gat (inkt ~25–30 % direct na elke zoom, bisect `pre`). Na U12 geen gat meer gemeten (≥ 0,3 s na moveend ~1×). Wat na U12 overblijft en als "fade/puls" leest: het synchrone cohort uit U12 (`c623612`, aanvullers `age = 1e-3`) — sterft boven land binnen 0,25 s en respawnt samen | na een zoom-in verdwijnt ~6 s later vrijwel het hele veld boven land tegelijk en fadet opnieuw in | fix 1 `a5242cd` (levensschaal 0,8–1,2 op afstand én maxAge, fase ook in leeftijd); fix 3 `4d05e7b` (gespreide aanvullers) |
| (2) heftiger i.p.v. minder | U19 (`ccb2688`) bereikt de PO niet: v2 (sinds U3b `44ac211`) schreef de hele tuning weg zodra één knop afweek, dus opgeslagen `intensity: 1.9` overschrijft de ⅔-default; windfocus maakte daar 2,85 van | rust-inkt met PO-tuning op main = 1,9-niveau (bisect `po-main`) | fix 2 `04c7633`: tuning v3 (alleen afwijkingen), v2 eenmalig gemigreerd; oude defaults volgen ⅔, eigen intensiteit ×⅔ → windfocus 1,27 → 1,9 en terug |
| (3) artefacten boven land | U12 (`c623612`): aanvullers van één zoomstap verschijnen allemaal in hetzelfde frame op leegste-celpunten (jitter 0,6) → rooster van even lange stompjes; boven land blijft het cohort daarna synchroon | `shots/voor-main-licht-inzoom-0.3s-zee.png` (rooster) vs `…-3.0s-zee.png` (normaal); `shots/bisect-*-in-0.3s.png` | fix 3 `4d05e7b` (volle celjitter, verschijnen gespreid over 0,15 s) + fix 1 |

Vóór/na-stills: `shots/vergelijk-{main,na}-{light,dark}-1-in-*.png` (zelfde moment ~1,9 s na inzoomen), bisect-stills `shots/bisect-{pre,u12,main}-*-in-0.3s.png`. Verdachten (b) buffer-blit en (d) halve pixel: niet waargenomen/niet gemeten als oorzaak. Bijvangst: `setTuning` draaide de viewport-herverdeling op elke focus-tweenframe — nu alleen bij dichtheidswijziging.

PO-punt 5 gedaan: `?dev` → "Reset alle instellingen" (wist alle `motregen-*`-keys behalve thema, favorieten, laatste favoriet, kaartview; zet wind/focus/isolijnen/labels/histogram/splash/temp-afstand/min.-breedte terug, maakt focus-pin los, wolkrand uit; melding "Standaardwaarden hersteld" 2,5 s); per-paneel-wind-reset blijft. Unit `dev-settings.test.ts`. Splash-slowdown schrijft de default niet meer weg.

Open met PO: (a) stuur "Kopieer als JSON" (`?perf=1`) uit je eigen browser zodat ik de migratie op je echte opslag kan controleren; (b) beoordeel de video's `video/{main,na}-*.webm` op het gevoel direct na inzoomen — op swiftshader-schaal is het verschil subtiel. Open voor agents: de e2e alsnog laten falen tegen pre-U12 (bewijs dat hij het gat vangt); spreiding 0,15 s is begrensd door de U12-pinchtest (0,3 s zakte onder 0,8×).
