[project]
name = "motregen-arome-spec"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "cfgrib>=0.9.15",
  "h5py>=3.16",
  "numpy>=2.0",
  "xarray>=2025.1",
]

[tool.uv]
package = false
